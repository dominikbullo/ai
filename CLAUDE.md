# ai

Skills are under `skills/`, agents under `agents/`.

## Skills

Organized into bucket folders under `skills/`:

- `personal/` — personal workflow skills (daily planning, GitHub, Claude sessions)
- `deprecated/` — no longer used

```bash
npx skills@latest add dominikbullo/ai
```

## Agents

Agent definitions under `agents/engineering/`, `agents/energy/`, `agents/productivity/`.

```bash
./scripts/link-agents.sh
```
